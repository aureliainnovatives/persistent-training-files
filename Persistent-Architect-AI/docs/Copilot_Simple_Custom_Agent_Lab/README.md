# Copilot Simple Custom Agent Lab

Start with `TRY-ME.md`.

This is a minimal standalone demonstration of a GitHub Copilot custom agent in VS Code.

No Python, API key, `.env`, LangChain, LangGraph, AutoGen, or CrewAI is required.
